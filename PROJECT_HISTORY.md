## v23 대시보드 복원 및 최종관리자 기준 보정

- 현재 제작 기준을 최종관리자 화면으로 되돌려 상단 영업 KPI는 전체 직원 데이터를 합산합니다.
- 향후 개인계정 로그인 시에만 상단 KPI 범위를 해당 담당자로 제한하도록 연결할 수 있게 주석/구조를 남겼습니다.
- 투두리스트는 상단 개인실적 범위와 분리해 재통화약속/상담 중/고려중 3종을 하나의 게시판형 리스트로 통합했습니다.
- v21까지 존재하던 분납 캘린더, 기일·제출기한 캘린더, 기간별 통계, 결제 구성, 사건 현황을 복원했습니다.

# 로파워 (LawPower) — 회생/파산 사건관리 어드민

도원 Admin(`tg_m`, 불법사채 구제센터 통합 업무관리) 소스코드를 그대로 참고해 만든
**개인회생/개인파산 사건관리 어드민** 프로토타입입니다. **디자인 시스템(컬러 토큰, 컴포넌트,
레이아웃)은 도원 Admin과 동일**하게 맞추고, 기능만 회생/파산 도메인에 맞게 새로 설계했습니다.
현재는 전부 **가상의 샘플 데이터**로 동작하며, 실제 DB(Supabase 등)는 연결되어 있지 않습니다.

## v13 업데이트 — 상담일지 필드 배치 정밀 재설계(좌/중/우 3열 + 회색 구분 바) (10차 정리 요청 반영)

v12에서 만든 3열 대형 팝업의 뼈대는 그대로 두고, 요청하신 정밀한 필드 배치도로 내부 구성을
전면 재정렬했습니다. 요청 원문의 4가지 표기 규칙(`[ ]`=수기 작성란, `[내용]`=요청사항,
7개 섹션명은 순수 "구분 바", 여백 최소화)을 그대로 따랐습니다.

**구분 바**: `[기본정보][소득][의사][자산][기대출리스트][기타][플랜]` 7개를 `SectionBar`
컴포넌트(회색 배경, 클릭·버튼 등 어떤 액션도 없는 순수 라벨)로 새로 만들었습니다. "+추가"
같은 버튼은 전부 바 바깥(본문 영역)으로 옮겼습니다.

**레이아웃**: CSS Grid auto-flow 대신 좌/중/우 3개 컬럼을 명시적인 `div`로 나눠 순서를
보장했습니다 — 좌측(기본정보 → 기타), 중단(소득 → 자산+채무요약), 우측(의사 →
플랜+최근대출·보험). 채무요약(총 채무금액 등 자동계산 타일)과 최근대출·보험 2줄은
요청하신 배치대로 별도 바 없이 각각 자산/플랜 섹션 안에 이어붙였습니다(자동계산 로직
자체는 전혀 바뀌지 않았습니다).

**신규 필드(additive)**: 기타 섹션의 "본인명의 다른 재산·개인채무·채무사실공유"
(`ConsultationPersonal`에 추가), 재직기간을 "첫 취직일(날짜) → 자동계산 개월수"로 입력받기
위한 `employmentStartDate`(`ConsultationIncome`에 추가, `calcTenureMonths`로 자동계산해
기존 `tenureMonths`/`tenureInfo`를 함께 채움), 나이는 기존 생년월일(`birthDate`) 필드를
그대로 활용해 `calcKoreanAge`로 만 나이를 참고 표시합니다(수동 입력값을 덮어쓰지 않음).
원금탕감율/변제금감소율은 숫자 직접입력 대신 "10~20%~90~100%" 구간 드롭다운
(`principalReductionRange`/`paymentReductionRange`)으로 바꿨고, 기존 숫자 필드는 과거
데이터 호환을 위해 그대로 남겨뒀습니다. 기대출리스트 구분1(드롭다운)에는 예시에 맞춰
"개인"을 추가했습니다(기존 "보증"/"기타"는 데이터 호환을 위해 유지).

**기대출리스트 재구성**: 파일 업로드가 "파일 선택 → (드래그 앤 드롭도 가능) → 추출 →
Preview 확인 → 적용"의 명시적 단계로 바뀌었습니다(이전에는 파일을 고르는 즉시 자동
분석했는데, 이번 요청의 "파일선택/추출/추가" 3버튼 구성에 맞춰 분석 시점을 "추출" 버튼
클릭 시점으로 분리했습니다). 표는 요청하신 대로 "구분1·구분2·금융사·실행일·잔액·관리"
6개 컬럼으로 간소화했습니다 — 월불입금·출처 등 나머지 데이터는 채무 요약 자동계산과
중복검사에 계속 쓰이므로 필드 자체는 남아있고, 이 표에서만 화면에 노출하지 않습니다.

**컴포넌트 정리**: 기존 `DebtSummary`/`DebtUploader`/`DebtTable`/`RecentLoanInsuranceSection`
4개 파일을 폐기하고, 그 내용을 각각 `AssetSection`(채무요약 편입), `DebtListSection`(업로드+
표 통합), `PlanSection`(최근대출·보험 편입)으로 흡수했습니다 — 파일 삭제 전 다른 곳에서
import하는 곳이 없는지 grep으로 확인했습니다. `ConsultationModal.tsx`·`app/db/page.tsx`·
`app/clients/page.tsx`가 이 컴포넌트들을 불러오는 방식(props)은 이번 라운드에서 전혀
바뀌지 않아, 두 화면(DB관리/고객관리) 모두 추가 수정 없이 새 레이아웃을 그대로 사용합니다.

**화면에서 뺀(데이터는 유지) 항목**: 소득 섹션의 "연금소득 여부·재직기간 상세 자유기재·
비고", 최근대출/보험의 "보험 관련 메모"는 이번 정밀배치 요청 목록에 없어 화면에서는
빼되, `ConsultationIncome`/`ConsultationRecentLoanInsurance`의 해당 필드 자체는 삭제하지
않았습니다(유입경로·기초정보메모 제거 때와 동일한 원칙 — 화면 노출만 제거, 데이터 보존).

**검증**: 이번에도 샌드박스 네트워크 정책상 `npm install`/`npm run build`를 실행할 수
없어(레지스트리 403 차단, 이하 v12 안내와 동일한 제약), 전역 TypeScript 컴파일러 +
직접 작성한 타입 스텁으로 `tsc --noEmit` 구조적 타입체크를 다시 수행했습니다 — **0
errors**로 통과했습니다. 로컬에서 `npm install && npm run build`로 최종 확인을
권장드립니다(아래 v12 안내와 동일).

## v12 업데이트 — 상담일지 3열 대형 단일팝업 전면 개편, 채무파일 자동분석, 콜 관리 경고 인라인화, 유입경로/기초정보메모 정리 (9차 정리 요청 반영)

**핵심 목표**: "인적사항 → 소득 → 자산 → 상담판단 → 채무 → 상담메모"를 화면을 오가지 않고
하나의 큰 팝업에서 한눈에 확인·작성할 수 있게 만드는 것. 아래는 요청하신 33개 항목 순서에
맞춰 반영 내용을 정리했습니다.

**1) 상담일지 팝업 구조 (요청 1)**: DB관리·고객관리 두 화면 모두 `[상담일지]` 버튼을 누르면
`min(96vw,1800px) x min(94vh,1050px)` 크기의 대형 단일 팝업(`ConsultationModal`)이 뜹니다.
`grid-template-columns: 1fr 1.05fr 1fr` 3열 CSS Grid로 기본정보/소득/상담판단(1행),
상담플랜/자산/최근대출·보험(2행)을 배치하고, 채무 요약/채무 파일업로드·리스트/상담메모는
전체 폭(3칸)으로 아래에 이어 붙였습니다. 탭이나 아코디언, 작은 팝업 중첩 없이 스크롤만으로
전체를 확인합니다. 1440px 이하에서는 폰트·간격을 살짝 줄이고, 그보다 좁은 화면에서는 팝업
내부 스크롤을 허용해 레이아웃이 깨지지 않게 했습니다.

**2~8) 기본정보 / 소득 / 자산 / 의사·상담판단 / 상담플랜 / 채무요약 / 최근대출·보험 (요청
2~8)**: 요청하신 필드를 각각 `BasicInfoSection` / `IncomeSection` / `AssetSection` /
`DecisionSection` / `PlanSection` / `DebtSummary` / `RecentLoanInsuranceSection` 컴포넌트로
분리 구현했습니다. 직군·거주형태 등 카테고리 선택과 O/X 판단값은 전부 빠른 선택형 세그먼트
버튼(`OXToggle`)으로, 금액은 "만원" 단위 입력(`ManwonInput`, 저장 시 내부적으로 원 단위로
환산해 기존 `fmtWon`/`computeRepaymentPlan` 로직과 호환 유지), 개월/명/% 등은 단위가 입력창
바깥에 별도 표시되는 `UnitNumberInput`으로 통일했습니다. 채무요약(총 채무·총 신용·총 담보·총
이자·월 불입금 등)은 채무리스트에서 자동 계산되며 천단위 콤마가 적용됩니다.

**9~10) 필수 작성란 · 고객전환 차단 (요청 9~10)**: 필수 항목은 `lib/consultation.ts`의
`REQUIRED_CONSULTATION_FIELDS` 설정 배열 하나로 관리하며(코드 곳곳에 하드코딩하지 않음),
라벨 배경을 연한 하늘색으로 표시하고 값이 비어있으면 연한 빨강 테두리 + 경고 아이콘을
보여줍니다. 고객전환은 프론트엔드 버튼만 막는 게 아니라 `lib/store.tsx`의
`convertLeadToClient` 함수 내부에서도 동일한 `checkConsultationRequired`를 다시 실행해,
콘솔 등에서 store 함수를 직접 호출해 버튼을 우회하더라도 필수값이 비면 상태 변경 없이
조용히 거부되도록 했습니다(현재 구조가 실제 백엔드 API 없이 클라이언트 in-memory
store이므로, "DB/API 레벨 validation"은 이 store 함수 레벨 재검증으로 구현했습니다 — 자세한
내용은 이 파일 하단 "중요 — 이번 작업의 아키텍처 관련 안내" 참고).

**11) 자동저장 대신 명시적 저장 유지**: 기존 프로젝트의 모든 팝업(분납입력, 전자약정서,
서류안내문, 이전 고객수정/상담일지 편집기 등)을 전수 조사한 결과 자동저장 사례가 전혀 없고
전부 명시적 `취소/저장` 버튼 구조였습니다. 요청하신 조건부 지침("기존 프로젝트가 명시적
저장 버튼 구조로 통일돼 있다면 기존 UX를 우선하라")에 따라 자동저장을 새로 도입하지 않고
기존과 동일한 `취소/저장` 버튼을 유지했습니다. 대신 요청하신 "상담일지 작성률 N% / 필수항목
N/M" 배지(요청 24)를 팝업 상단에 실시간으로 표시합니다.

**12~14) 채무파일 첨부·파싱·정규화 (요청 12~14)**: 파일 선택 + Drag & Drop을 모두 지원하는
`DebtUploader`를 추가했습니다. `.xlsx`/`.xls`/`.csv`는 `xlsx` 패키지로 실제 파싱하고,
`.pdf`는 이번 라운드에서 실제 파싱기를 붙이지 않고 "PDF는 아직 자동 파싱을 지원하지 않으니
직접 입력해달라"는 안내로 우아하게 처리합니다(요청하신 대로 셀 주소를 하드코딩한 일회성
파서 대신 `lib/debt-parsers/providers/*` provider-adapter 구조로 만들었고, 나중에 실제
`.pdf` 샘플이 오면 provider를 하나 더 추가하는 방식으로 확장 가능). 업로드 즉시 저장하지
않고 "분석 → 추출 결과 Preview(N건) → 사용자가 적용 버튼을 눌러야 실제 반영" 흐름을
그대로 구현했습니다. 내부 데이터는 원본 파일의 컬럼명과 무관하게 `LoanRecord`(요청하신
`DebtItem` 형태와 동일한 필드 구성)로 정규화됩니다.

**15~17) 채무 수기입력 · compact 테이블 · 중복검사 (요청 15~17)**: `[+ 수기로 추가]`
버튼으로 행을 바로 추가할 수 있고, 삭제는 기존 프로젝트 전체(리드/고객/게시글/첨부파일 등)를
조사한 결과 소프트 삭제·비활성화 관행이 전혀 없어(전부 즉시 hard delete) 동일하게 hard
delete로 구현했습니다. 리스트는 `max-h-64` 내부 스크롤이 걸리는 compact 테이블
(`DebtTable`)로, 채무가 많아져도 팝업 전체 길이에는 영향이 없습니다. 파일에서 추출한 항목은
금융사·대출종류·실행일·원금·잔액이 기존 채무와 비슷하면 "기존과 유사(중복 의심)" 표시와
함께 기본적으로 적용 체크가 꺼진 상태로 Preview에 나타나, 사용자가 직접 확인 후 추가
여부를 선택합니다.

**18~20) 상담 메모 게시판 · 데이터 구조 · DB관리 목록 연동 (요청 18~20)**: v11에서 먼저
구현했던 누적 게시판형 메모(`ConsultationMemoSection`, `ConsultationInfo.memoLog`)를 이번
대형 팝업에도 그대로 유지·통합했습니다. 작성자·시각은 사용자가 입력할 수 없고 항상
`CURRENT_STAFF`(로그인 담당자에 해당)와 `new Date()`로 자동 기록되며, 표시 형식은
`YYYY-MM-DD HH:mm:ss`(초 단위까지)입니다. DB관리 목록 맨 오른쪽 "메모" 칼럼(`LeadMemoCell`)
을 클릭하면 지금까지 쌓인 메모 전체를 팝오버로 조회만 할 수 있고, 새 메모 작성은 요청하신
대로 상담일지 팝업에서만 가능합니다.

**21) 기존 상담데이터 호환 (요청 21)**: `ConsultationInfo`(`lib/types.ts`)의 기존 필드
(인적사항/소득현황/채무현황/상담내용 등)는 하나도 삭제하지 않았고, v12에서 추가한 모든
필드(나이·거주지역·회사지역·4대보험 여부·재직기간·거주형태·의사판단·상담플랜·`housing`/
`judgment`/`counselPlan`/`debtSummaryExtra`/`recentLoanInsurance` 등)는 전부 **선택
필드(optional)로 additive 추가**했습니다. 기존에 저장돼 있던 고객(`clients[0]` 샘플 등)의
상담일지 데이터는 새 팝업에서도 그대로 이어서 보이고 수정됩니다.

**22~24) 입력 UX · 필수값 디자인 · 완료도 표시 (요청 22~24)**: 금액은 천단위 콤마, 단위는
입력창 밖에 별도 표시, 필수 라벨은 연한 하늘색/일반 라벨은 회색으로 구분했습니다. 팝업 상단에
"상담일지 작성률 N%"와 "필수항목 N/M" 배지를 표시하되, 요청하신 대로 고객전환 가능 여부
판단은 작성률이 아니라 필수항목 충족 여부(`checkConsultationRequired`)로만 결정합니다.

**25~27) 디자인 방향 · 컴포넌트 분리 · 성능 (요청 25~27)**: 참고 이미지의 3열 고밀도 배치·
O/X 선택·단위 분리 입력·하단 채무리스트 구조는 그대로 참고하되, 색상·타이포·버튼 스타일은
현재 어드민(도원 Admin과 동일한 디자인 시스템)의 카드/버튼/포커스링 스타일을 그대로
따랐습니다. `ConsultationModal.tsx` 한 파일에 몰아넣지 않고 `components/ui/consultation/`
아래 12개 파일(BasicInfoSection/IncomeSection/AssetSection/DecisionSection/PlanSection/
RecentLoanInsuranceSection/DebtSummary/DebtUploader/DebtTable/ConsultationMemoSection/
shared/ConsultationModal)로 분리했습니다. 현재 프로젝트는 실제 API 서버가 없는 in-memory
mock 구조라 "여러 API를 순차 요청" 문제 자체가 발생하지 않으며, 메모·채무 한 건 추가도
React state만 갱신하고 페이지 새로고침 없이 반영됩니다.

**28~29) 파일 업로드 보안 · 파싱 실패 처리 (요청 28~29)**: 허용 확장자(.xlsx/.xls/.csv/.pdf)
와 파일 크기(20MB 이하)를 검사합니다. 다만 현재 프로젝트는 실제 서버/Storage가 없는 프런트
전용 mock 구조라 "Storage 파일명 UUID화·URL 무단 접근 차단"은 실제 서버가 붙기 전까지는
해당 사항이 없고, 실서비스 연동 시 함께 구현해야 할 항목으로 아래 "다음 단계" 섹션에
남겨두었습니다. 파싱에 실패해도(형식 불명, 추출 실패 등) 상담일지 전체가 깨지지 않고
"자동으로 확인하지 못했습니다 — 다시 시도 / 수기 입력" 안내로 처리되며, 확신할 수 없는 값은
추측해서 채우지 않고 사용자 확인을 거치도록 했습니다.

**30~32) 개발 순서 · 금지사항 · 최종 사용자 흐름 (요청 30~32)**: 요청하신 순서(구조 분석 →
필드/DB 매핑 정리 → migration 설계 → 대형 팝업 구현 → 기존 데이터 연결 → 필수값 validation
→ 고객전환 연동 → 수기입력 → 업로드/파싱 → 메모 이력 → DB목록 메모 팝업 → regression)대로
진행했고, "절대 하지 말 것" 목록(기존 데이터 삭제, 기존 테이블 이유 없는 교체, 기존 기능
제거, mock만 넣고 완료 처리, 프론트 전용 validation, 업로드 즉시 저장, 자동확정, 하드코딩
파서, 메모 이어붙이기, 팝업 중첩 등)을 모두 지켰는지 확인했습니다.

**추가로 반영한 3가지 요청**:
- **콜 관리 경고 인라인화**: 별도 상단 배너 Card를 없애고, DB관리 고객리스트 각 행(모바일
  카드형/데스크톱 표형 모두)의 이름 옆에 `CallWarningBadge`로 바로 표시되도록 바꿨습니다.
  판정 로직(`checkCallWarning`) 자체는 그대로입니다.
- **유입경로 UI 제거**: DB관리 화면의 유입경로 필터·칼럼·행별 선택 UI를 모두 제거했습니다.
  다만 요청하신 대로 `lib/types.ts`의 `LeadSource` 타입과 `lib/mock-data.ts`의 데이터는
  전혀 건드리지 않았습니다(데이터 모델은 그대로 두고 화면 노출만 걷어낸 것) — 추후 다시
  보여줘야 하면 화면 쪽만 되돌리면 됩니다.
- **기초정보메모 입력란 제거**: DB관리 목록에서 자유 텍스트 `lead.memo`를 바로 쓰던 입력창을
  모바일/데스크톱 두 화면 모두에서 제거했습니다. 게시판형 메모 조회 기능(`LeadMemoCell`,
  위 18~20 참고)은 별개 기능이라 그대로 유지했습니다.

**중요 — 이번 작업의 아키텍처 관련 안내**: 이번 라운드를 시작하기 전, 실제 Supabase
연동으로 전환할지 여부를 여쭤봤고 "기존처럼 in-memory 목업 구조 유지"로 답변을 주셔서 그
방향으로 진행했습니다. 그에 따라 이 프로젝트 전체와 마찬가지로 v12도 실제 DB가 없으므로,
33번 항목에서 말씀하신 "DB migration/신규 테이블·컬럼"은 TypeScript 인터페이스에 optional
필드를 추가하는 additive 변경으로, "DB/API 레벨 validation"은 `lib/store.tsx`의
`convertLeadToClient` 함수 내부 재검증으로, "별도 메모 테이블"은 `ConsultationInfo.memoLog:
MemoLogEntry[]` 배열 구조로 각각 구현했습니다.

**production build 관련 중요 안내**: 이번 작업 환경(샌드박스)의 네트워크 정책상
`registry.npmjs.org`를 비롯한 npm 패키지 레지스트리·CDN에 접근이 차단되어 있어(조직
egress 정책 위반으로 403 거부), `npm install`/`npm run build`를 실제로 실행할 수
없었습니다. 이 문제를 몰래 우회하거나 없었던 일처럼 보고하지 않고, 대신 다음 방법으로
최대한 검증했습니다: 전역 설치된 TypeScript 컴파일러(6.0.3)와, react/next/lucide-react
등 외부 패키지의 실제 타입 선언 대신 직접 작성한 permissive 타입 스텁을 이용해
`tsc --noEmit` 구조적 타입체크를 수행했고, 이 과정에서 실제 버그 1건(`ConsultationModal.tsx`
의 `footerLeft`/`onExportExcel` 파라미터명 불일치)을 발견해 수정했습니다. 최종적으로 이
방식의 타입체크는 **0 errors**로 통과했지만, 이는 실제 `npm run build`(Next.js 프로덕션
번들링, ESLint 등 포함)를 대체하지 않습니다. **로컬 환경에서 반드시 `npm install && npm
run build`를 한 번 실행해 최종 확인해주시길 권장드립니다.**

## v11 업데이트 — 상담메모 게시판, 콜 관리 경고(태그 기반), 상세 DB관리, 상담일지 팝업 확대 (8차 정리 요청 반영)

**1) 상담일지 "상담메모" 탭 → 게시판형 누적 로그로 교체**: 기존 단일 텍스트란(덮어쓰기 방식)을
없애고, 메모를 쓰고 [추가]를 누를 때마다 "누가 · 언제(연-월-일 시:분:초) · 무슨 내용"이 맨
위에 쌓이는 게시판 형태(`ConsultationInfo.memoLog`)로 바꿨습니다. DB관리·고객관리 두 화면의
상담일지 팝업 모두 동일한 게시판을 공유하며, DB관리 목록의 맨 우측 메모 칸을 클릭하면
지금까지 쌓인 메모 전체를 팝오버로 펼쳐 볼 수 있습니다(`LeadMemoCell`). 엑셀 다운로드에도
작성자·시각·태그·내용이 표(시간순)로 그대로 반영됩니다.

**2) 콜 횟수·재통화 예정일 삭제 → [재통화]/[부재중] 태그 기반 "콜 관리 경고"로 전면 교체**:
콜카운터(▲▼)와 재통화 예정일 필드를 완전히 삭제했습니다. 대신 메모 작성란 아래에 [재통화]
/[부재중] 토글 버튼을 추가해, 태그를 선택한 뒤 [추가]를 누르면 그 기록이 오늘(한국시간
KST 기준) 콜 관리 판정에 쓰입니다 — 오늘 [재통화](통화 연결 성공)가 한 번이라도 있으면
즉시 경고가 사라지고, 없어도 [부재중]이 3건 쌓이면 "오늘 할 만큼 했다"고 보고 역시
사라집니다. 자정(KST 00:00, 브라우저 로컬 시간대와 무관하게 UTC+9로 직접 계산)이 지나면
모든 판정이 초기화되어 매일 새로 경고가 뜹니다(`lib/consultation.ts`의 `checkCallWarning`).
DB관리 상단의 "담당자별 오늘 콜 현황" 패널은 이 로직에 맞춰 "오늘 콜 관리 경고" 박스로
교체됐고, 대시보드의 관련 KPI·투두리스트도 같은 판정 기준으로 다시 계산됩니다.

**3) 리드정보·광고 인스턴트 양식 피벗 — 색상 카드 → 세로 텍스트로 단순화**: DB관리 목록의
"리드정보(인스턴트양식)" 칸이 가로로 나열된 색상 카드였던 것을, 색상 없이 세로로 나열되는
일반 텍스트로 바꿨습니다(`LeadTags`). 상단의 "광고 인스턴트 양식 피벗" 필터바도 카테고리별
색상을 모두 제거하고, 선택 여부만 진하게/연하게로 구분되는 중립 톤 텍스트 카드로 바꿨습니다
(`PivotBar`).

**4) 신규 메뉴 "상세 DB관리"**: 사이드바 메뉴 순서를 요청하신 순서(대시보드/DB관리/상세
DB관리/고객관리/계약관리/입금분납/게시판/정산/정산설정/최저생계비 계산기/기간별
변동내역/데이터집계)로 재배치하고, DB관리 바로 다음에 "상세 DB관리"를 새로 추가했습니다.
상담일지에 "상세 단계" 드롭다운(착수/서류/법원/워크아웃 4개 트랙, 트랙별 6~7개 세부
단계 — 참고 이미지의 분류 체계를 그대로 옮김)을 추가해 상담원이 고객을 단계별로 지정할 수
있고, 상세 DB관리 화면에서는 참고 이미지 스타일의 타일(트랙별로 묶여 건수·비율 표시)을
보여주다가, 타일을 클릭하면 그 아래에 해당 단계로 분류된 고객이 게시판 형식으로 나타납니다.

**5) 상담일지 팝업 — 제목 단순화 · 안내문구 삭제 · 팝업 확대**: 팝업 최상단 제목을
"{이름} · 상담일지" 형태로 단순화하고, "고객 전환 전이라도…" 안내 문구를 삭제했습니다.
팝업 크기도 기존 대비 훨씬 크게(`Modal size="full"`, 최대 1800px) 키우고, 6개 섹션을
세로 1단이 아닌 2단 그리드로 배치(내용량이 많은 채무현황·상담메모만 2칸 전체 사용)해
브라우저 창을 넘지 않는 선에서 한 화면에 훨씬 많은 내용이 동시에 보이도록 했습니다 —
다만 채무현황 표 등 항목 자체가 많아 완전한 "스크롤 0"까지는 아니며, 스크롤량을 최대한
줄이는 선에서 구성했습니다.

## v10 업데이트 — 고객전환 게이트, 유입경로 세분화, 콜 체계화, 상담기록지 단일팝업 전면 개편 (7차 정리 요청 반영)

**1) "실제 계약(=상담기록지 필수항목 완료) 전에는 고객관리로 넘기지 않도록" 전환 게이트 추가**:
`lib/consultation.ts`에 `checkConsultationRequired()`를 새로 만들어, 상담 후 방향(개인회생/
개인파산/워크아웃)·거주지·직업·월평균소득·총 채무금액(채무현황 표 또는 기대출 리스트 중
하나) 5개 핵심 항목이 모두 채워졌는지 검증합니다. DB관리의 "고객 전환" 버튼은 이제 이 검증을
먼저 통과해야만 실제 전환을 실행하고, 비어있으면 전환 대신 어떤 항목이 빠졌는지 알려주는
안내 배너와 함께 상담일지 팝업을 열어줍니다. 목록 화면에서도 필수항목이 비어있는 리드에는
"필수항목 미입력" 표시가 붙습니다.

**2) 고객 DB 유입경로 세분화**: `LeadSource` 타입(메타 광고/네이버 검색광고/네이버 블로그·
카페/유튜브 광고/당근마켓/지인소개/재방문/제휴사 연계/기타 9종)을 새로 추가했습니다.
DB관리 목록·필터·상세 각 행에서 유입경로를 직접 지정할 수 있고, 필터 드롭다운으로 채널별
DB 수를 바로 걸러볼 수 있습니다 — 광고 채널별 반응률/전환율을 나중에 비교하기 위한 기초
필드입니다.

**3) 콜 체계화 — 일일 최소 콜 횟수 기준 + 업무 강제성**: 콜카운터의 ▲(실제 통화 시도)
버튼을 누를 때마다 store에 `callLog` 이벤트가 하나씩 쌓이도록 바꿔(▼ 보정 버튼은 로그를
남기지 않음), "오늘 하루 담당자별로 몇 건 통화를 시도했는지"를 정확히 집계할 수 있게
했습니다. DB관리 상단에 "담당자별 오늘 콜 현황" 패널을 추가해 관리자가 설정한 일일 목표
(기본 30건) 대비 달성/부족 건수를 담당자별로 바로 보여줍니다. 전체 관리자 입장에서 영업진의
콜 활동을 감시·독려할 수 있는 최소한의 업무 강제성 장치입니다.

**4) 본인신용정보 열람서비스 연동**: 실제 자동 파싱(OCR/스크래핑)은 이 데모 환경에서는
지원하지 않지만, 한국신용정보원이 운영하는 공식 서비스 "크레딧포유"(credit4u.or.kr) 링크를
상담일지 채무현황 탭에 바로가기 버튼으로 추가해, 상담원이 원클릭으로 열람 페이지로 이동한 뒤
다운로드한 채무내역 파일을 바로 첨부할 수 있게 했습니다.

**5) 상담기록지 팝업 전면 개편 — 탭 전환 → 한 화면(단일 팝업)**: 요청하신 참고 화면(기존
상담기록지)처럼, DB관리의 "상담일지 작성" 팝업을 탭을 눌러가며 이동하는 방식에서 인적사항·
소득현황·재산현황·채무현황·변제계획·상담메모 6개 섹션을 모두 한 화면에 세로로 쌓아 한눈에
보이도록 바꿨습니다. 필수 항목(거주지/직업/월평균소득/총 채무금액/상담 후 방향)은 하늘색
박스로 강조 표시되고, 팝업 상단에는 전체 완료 여부와 누락 항목 목록을 보여주는 요약 배너를
추가했습니다. (고객관리의 고객정보 수정 팝업은 기존 탭 방식을 그대로 유지했습니다 — 이번
요청은 "DB관리에서 상담일지 눌렀을 때"로 한정되어 있었습니다.)

**6) 기대출 리스트 + 파일첨부**: 기존 5개 고정 카테고리 채무현황 표와는 별도로, 참고 화면의
"기대출 리스트"처럼 구분1(신용/담보/보증/기타)·구분2(상품명)·금융사·실행일·잔액·비고를
개별 대출 건마다 "수기로 추가" 버튼으로 하나씩 등록/삭제할 수 있는 상세 목록을 추가했습니다.
그 아래에는 파일을 끌어다 놓거나 클릭으로 선택해 첨부할 수 있는 첨부파일 영역을 만들어,
본인신용정보 열람서비스에서 받은 채무내역 파일을 붙여둘 수 있게 했습니다 — 자동 "내용 추출"
버튼은 이 데모에 업로드/OCR 서버가 없어 아직 지원하지 않는다는 안내만 표시되며(정직하게
스텁으로 표시), 위 "수기로 추가" 버튼으로 실제 입력은 완전히 동작합니다.

## v9 업데이트 — 관리필요 박스 전체 건수 표시, 재통화 안내문구 겹침 수정 (6차 정리 요청 반영)

**1) "관리가 필요한" 박스가 전체 건수를 다 보여주지 않던 문제**: DB관리·고객관리 상단의
"관리가 필요한" 박스가 뱃지에는 전체 건수(예: 22건)를 표시하면서 실제로는 가로 스크롤
카드를 12건까지만 잘라 보여줘, 나머지가 있는지 알아채기 어려웠습니다. 가로 스크롤(숨겨진
스크롤바) 카드 방식에서, 게시판처럼 세로로 전부 나열하고 박스 안에서 세로 스크롤(스크롤바
보임)되는 리스트 방식으로 바꿔 — 건수 제한 없이 전체가 스크롤을 통해 모두 보이도록
했습니다.

**2) 재통화 예정일 안내 문구가 옆 컬럼과 겹쳐 보이던 문제**: v8에서 표 글씨 세로쌓임
버그를 고치며 표 셀 전체에 `white-space: nowrap`을 기본 적용했는데, DB관리 표의
"재통화 예정일" 컬럼 안내 문구("미지정 → OOOO-OO-OO 제안(클릭해서 지정)")처럼 원래
한 줄에 다 들어가지 않는 긴 문구는 nowrap 때문에 줄바꿈되지 못하고 옆 담당자/상태
컬럼 위로 넘쳐 겹쳐 보였습니다. 이 문구에만 다시 줄바꿈을 허용(`whitespace-normal`)해
컬럼 안에서 자연스럽게 2줄로 접히도록 수정하고, 컬럼 폭도 조금 넉넉하게 넓혔습니다.

## v8 업데이트 — UI 세로쌓임 버그, 피벗 토글, 정산설정 분리, 업무일정 캘린더 등 5차 정리 요청 반영

**1) 텍스트 세로쌓임(줄바꿈) 버그 수정**: DB관리 등 데스크톱 표 레이아웃이 화면 너비에 따라
컬럼이 좁아질 때, `white-space: nowrap` 보호 규칙이 모바일 미디어쿼리(`max-width:767px`)
안에만 있어서 그 경계를 벗어난 화면(가로모드, 큰 화면 폰, 창을 좁힌 데스크톱 브라우저 등)
에서는 이름·연락처 같은 글자가 한 글자씩 세로로 줄바꿈되는 문제가 있었습니다.
`app/globals.css`의 `.admin-responsive-table th, td` 규칙을 화면 너비와 무관하게 항상
적용되도록 바꿔, 모바일(안드로이드/아이폰) · 데스크톱 어디서든 글씨가 항상 가로 한 줄로
표시됩니다.

**2) DB관리 피벗칩 토글**: 상담가능시간 등 피벗 칩을 클릭해 필터링한 뒤, 같은 칩을 다시
클릭하면 선택이 해제되고 "전체"로 돌아가도록 했습니다.

**3) 리드정보 한 줄 표시**: 채무총액·소득·상담시간 태그가 2줄로 줄바꿈되던 것을, 한 줄로
나열하고 넘치면 가로 스크롤되도록 바꿨습니다(스크롤바는 보이지 않게 처리).

**4) 정산설정 독립 메뉴 + 결제방식 연동**: 예전에는 고객관리 안 팝업에서만 담당자×결제수단
요율을 조정할 수 있었는데, 사이드바에 "정산설정" 메뉴를 새로 만들어 관리자가 언제든 요율을
관리할 수 있게 분리했습니다. 또한 그동안 읽기전용으로만 표시되던 "결제방식"을, 고객관리
수정 화면에서 영업진이 실제로 드롭다운으로 선택할 수 있게 바꿨습니다 — 선택한 결제방식은
정산설정에서 설정한 요율과 자동으로 곱해져 **정산 메뉴의 "담당자별 정산 요약"에 예상
정산액 컬럼**으로 바로 반영됩니다.

**5) 대시보드 업무일정 캘린더**: 기존 분납/기일 캘린더 아래에 "업무일정 캘린더(DB 재통화
예정)"를 새로 추가했습니다. DB관리에서 지정한 재통화 예정일이 달력에 표시되고, 예정일이
지난 건은 "지남" 배지로 눈에 띄게 표시됩니다.

**6) DB관리·고객관리 상단 "관리가 필요한" 박스**: 도원 사채어드민의 "분납/상환일정 미등록"
박스와 같은 취지로, DB관리에는 재통화 예정일이 지났거나 아직 지정되지 않은 리드를,
고객관리에는 미수금은 남아있는데 분납일정이 하나도 등록되지 않은 의뢰인을 각각 상단에
가로 스크롤 카드로 모아 보여줍니다. 카드를 클릭하면 해당 건이 바로 필터링/선택됩니다.
이 어드민의 취지(① 관리자의 영업진 업무 모니터링, ② 영업진 스스로의 놓침 방지·효율적
고객 관리)에 맞춰, 관리자와 영업진 모두 "지금 무엇을 챙겨야 하는지"가 한눈에 보이도록
설계했습니다.

⚠️ 참고: 이번 라운드에서는 실제 소스 코드 반영에 집중했고, 정적 미리보기 이미지
(`dashboard-preview.html/png`)는 갱신하지 않았습니다 — 실제 동작은 `npm run dev`로 직접
확인해주세요.

## v7 업데이트 — 광고 인스턴트 양식, TM 영업 대시보드, DB 상담일지 등 4차 정리 요청 반영

**1) 광고 인스턴트 양식 필드 + 피벗**: 메타 광고 인스턴트 양식의 고정 질문 3종(채무 총금액·
실 월소득·상담가능시간)을 DB 리드 스키마에 고정 필드로 추가했습니다. DB관리 리스트에
색상 태그로 표시되고, 상단 피벗 바에서 각 카테고리(특히 요청하신 상담가능시간대)를
클릭하면 그 그룹의 리드만 걸러볼 수 있습니다. ⚠️ 예전에 쓰시던 "매일법률사무소 DB
구글시트 DB가공" 탭의 실제 색상표에는 접근할 수 없어, 카테고리가 한눈에 구분되도록
유사한 톤으로 새로 설계했습니다 — `lib/types.ts`의 `DEBT_RANGE_COLOR` /
`INCOME_RANGE_COLOR` / `CONSULT_TIME_COLOR` 값만 바꾸면 원하는 배색으로 바로
바뀝니다.

**2) 대시보드 — TM 영업 관점 지표**: 계약·분납 정보보다 오늘 해야 할 일이 먼저 보이도록
대시보드 맨 위에 당일 신규 DB, 전일 부재중, 5회 이하 컨택 부재율, 이번달 신규DB 대비
선임수·선임률 4개 지표와 "고려중 의뢰인 재설득 컨택 투두리스트"(재통화 예정일이 지난
순으로 정렬, 클릭하면 DB관리로 이동)를 추가했습니다. ⚠️ "전일 부재중"은 통화 시도
이력을 별도로 기록하는 콜 로그가 아직 없어 "어제 접수된 리드 중 현재 부재중 상태인
건수"로 근사했습니다 — 정확한 통화 이력이 필요하시면 콜 로그 테이블을 별도로 추가하는
작업이 필요합니다.

**3) 신청분류 → 상담 후 방향(개인회생/개인파산/워크아웃)**: DB 접수 시점에 미리 정하던
"신청분류"를 없애고, 상담원이 상담을 마친 뒤 방향을 고르는 "상담 후 방향"으로 시점과
명칭을 바꾸면서 워크아웃 옵션을 추가했습니다. 법원 사건(계약관리)은 여전히 회생/파산만
다루므로 계약관리 쪽 데이터 구조는 그대로 두었고, DB·고객 단계의 분류용 필드만
3종으로 넓혔습니다. 고객관리 상단 탭에도 워크아웃 필터가 추가됩니다.

**4) DB관리에서 상담일지 작성**: 이전에는 고객관리로 전환한 뒤에만 상담일지를 쓸 수
있었는데, 이제 DB 단계에서도 "상담일지 작성" 버튼으로 같은 화면(인적사항/소득현황/
재산현황/채무현황/변제계획/상담메모)을 바로 작성할 수 있습니다. 여기서 작성한 내용은
나중에 "고객 전환"을 누르면 그대로 고객관리로 승계됩니다. (내부적으로 고객관리 수정
팝업과 동일한 컴포넌트를 공유하도록 리팩터링해, 두 화면의 상담일지 UI가 항상 같게
유지됩니다.)

**5) 재통화 예정일 관리**: DB 리드마다 재통화 예정일을 지정할 수 있고, 비워두면
"접수일+1일"을 기본 제안값으로 보여줍니다. 예정일이 오늘이거나 지나면 리스트에서
바로 눈에 띄게 표시되고, 이 정보가 대시보드의 "고려중 재설득 투두리스트" 정렬 기준으로도
쓰입니다.

**6) 담당자 필터**: DB관리에 담당자별 필터를 추가해, 관리자가 전체 담당자 또는 특정
담당자의 DB만 골라볼 수 있습니다. ⚠️ 말씀하신 "담당자 배정 시 그 담당자 화면에서만
보이게"는 로그인 권한에 따라 접근 자체를 제한하는 기능이라, 이후 진행하기로 하신
권한 시스템(최고관리자/중간관리자/담당자별 열람·수정 권한) 작업 때 함께 구현하는 게
맞다고 판단해 이번에는 필터 기능까지만 넣었습니다. '상태' 값 목록 정리도 말씀하신 대로
이번엔 손대지 않았습니다.

**7) 계약금액/결제금액/미수금 3분할 표시**: 고객관리·계약관리의 목록과 상세 화면
전반에서 계약금액/결제금액/미수금 세 금액이 같은 용어로 일관되게 보이도록 정리했습니다
(계약관리 목록·고객관리 목록에 결제금액 컬럼을 추가하고, "기납부액"으로 불리던 곳도
"결제금액"으로 통일).

## v6 업데이트 — 서류안내문·정산요율·최저생계비 계산기 등 3차 정리 요청 반영

**1) 서류안내문 전송**: 고객관리 상세 화면(고객정보 클릭)에서 "서류안내문 전송" 버튼을 누르면
팝업이 뜨며, 도원 Admin의 '카카오톡 공지 생성' 기능을 참고해 만들었습니다. 서류체크리스트에서
아직 체크되지 않은 항목을 자동으로 뽑아 안내 메시지 초안을 만들어주고, 발송 채널(카카오톡
알림톡/SMS)과 수신 연락처를 선택해 전송할 수 있습니다. ⚠️ 카카오톡 알림톡/SMS 발송 API 연동
전이라 전송 버튼은 실제로 발송되지 않는 미리보기 화면이며, 실서비스 전환 시 API만 연결하면
그대로 동작합니다.

**2) 결제수단별 정산금 / 담당자별 정산요율 설정**: 결제수단(단순분납/로피분납/신카할부완납/
캐피탈분납)에 따라 정산금이 달라지고, 같은 결제수단이라도 담당자별로 다른 요율을 적용할 수
있어야 한다는 요청을 반영해 고객관리 상세 화면에 "정산 설정" 버튼(팝업)을 추가했습니다. 담당자
× 결제수단 표에서 요율(%)을 직접 입력하면 즉시 저장되고, 선택된 고객의 계약금액 기준 예상
정산금도 함께 보여줍니다. 위 두 버튼은 요청하신 대로 기존 고객정보수정/분납관리/전자계약서
버튼과 같은 자리(고객정보 클릭 시 뜨는 상세 화면)에 배치했습니다.

**3) 계약현황 표기 분리**: 고객관리 목록의 계약현황 컬럼이 "개인회생 · 진행중"처럼 한 덩어리로
보이던 것을, 요청하신 대로 **"계약분류 | 진행도"** 형태로 분리 표기하도록 바꿨습니다.

**4) 최저생계비 계산기(신규 메뉴)**: 가구원수별 최저생계비를 로펌 관리자가 직접 설정/관리하는
전용 메뉴를 새로 만들었습니다. 여기서 설정한 금액은 고객관리 수정 팝업의 변제계획 탭에서
가구원수를 입력하는 즉시 자동으로 반영됩니다(직접 수정도 가능). v5에서 설명드린 것처럼 이
수치는 법적으로 민감해 임의로 채워두지 않았으므로, 처음 사용 시 관리자가 최신 고시 기준을 한
번 입력해두어야 합니다.

**5) 메뉴 순서 조정**: 게시판 메뉴를 정산과 기간별 변동내역 사이로 옮겼습니다. 최종 순서는
대시보드 → DB관리 → 고객관리 → 계약관리 → 입금·분납 → 정산 → **게시판** → 기간별 변동내역 →
데이터집계 → **최저생계비 계산기** 입니다.

**6) 계약관리 유형/단계 표기 변경**: 박스(배지) 형식이던 유형·단계 컬럼을 요청하신 대로 일반
텍스트로 바꿨습니다(상태 컬럼의 배지는 유지).

**7) 고객관리 버튼/다운로드**: 목록 맨 우측 버튼을 "수정"에서 **"상세"**로 이름을 바꿨습니다
(클릭 시 뜨는 화면은 기존과 동일한 고객정보 수정 팝업입니다). 그 팝업 안에서 상담일지 전체
내용을 엑셀로 깔끔하게 내려받을 수 있는 **"고객 상담 엑셀 다운로드"** 버튼을 추가했습니다 —
처음 보내주셨던 상담일지 서식과 동일한 섹션 구성(인적사항/소득현황/재산현황/채무현황/변제계획
자동계산/상담메모)으로 `.xlsx` 파일이 즉시 생성/다운로드됩니다. ⚠️ 이 기능은 `xlsx`(SheetJS)
패키지를 새로 추가한 것이라, 실제 실행 전에 `npm install` 한 번이 필요합니다(아래 "처음 실행
하기 전에" 참고).

**8) DB관리 콜 횟수**: DB관리 리스트에 "콜 횟수" 항목을 추가했습니다. 처음엔 0에서 시작하고,
우측의 ▲▼ 버튼을 클릭할 때마다 숫자가 바로 증감합니다(별도 입력 없이 클릭만으로 기록).

**참고 — 지금은 최고 관리자 화면입니다**: 현재 만들고 있는 것은 모든 메뉴를 다 볼 수 있는 최고
관리자 페이지이고, 이후 로그인 권한별로 게시판 열람이나 수정 권한 등을 다르게 설정하는 작업은
별도로 진행 예정이라고 말씀해주신 내용 확인했습니다 — 이번 라운드에서는 별도로 구현하지
않았고, 다음 작업으로 남겨두었습니다.

## v5 업데이트 — 상담일지 반영 + 2차 정리 요청 반영

**1) 상담일지(개인회생·개인파산 상담일지) 전체 반영**
전달해주신 상담일지 엑셀 서식(인적사항/소득현황/재산현황/채무현황/변제계획 자동계산/상담메모)을
고객관리 **수정 팝업**에 8개 탭(기본정보/인적사항/소득현황/재산현황/채무현황/변제계획/상담메모/
서류체크리스트)으로 그대로 반영했습니다. 재산·채무 항목은 서식의 고정 행 구성(주택/토지, 임차
보증금, 예금/적금, 보험, 자동차, 퇴직금, 주식/펀드, 기타 / 세금체납, 건강보험체납, 담보채무(부동산·
자동차), 신용채무)을 그대로 사용했고, 5번 섹션의 자동계산 로직(월 가용소득 → 총변제예정액 → 청산
가치 비교 → 최종 변제금 → 탕감액/탕감률 → 진행가능여부)도 서식 그대로 구현해 입력하는 즉시
계산됩니다. 각 탭 하단에는 서식의 "상담 가이드" 체크포인트도 요약해 넣었습니다.

⚠️ **한 가지 양해 부탁드리는 부분**: 서식의 "최저생계비"(가구원수별)와 "소액임차인 최우선변제"
(지역별) 기준액은 매년/지역별로 고시가 바뀌는 법적으로 민감한 수치라, 임의의 표를 만들어 자동
판정하지 않았습니다. 최저생계비는 1인가구 기준값(1,538,543원, 전달주신 서식 원문 수치)만 참고로
채워두고 나머지는 상담원이 최신 고시를 확인해 직접 입력하도록 했고, 소액임차인 최우선변제는
자동조회 대신 참고 메모 필드로 남겨뒀습니다. 실서비스 전환 시 두 기준표를 법무팀에서 확정해주시면
자동화하겠습니다.

**2) 메뉴**: 도원 Admin과 동일한 구조로 **정산 / 기간별 변동내역 / 데이터집계** 3개 메뉴를 새로
추가했습니다. 사채업 전용 지표(환수, 연체율, 재약정성공률 등)는 로파워 도메인에 맞지 않아 계약매출·
실매출·미수금, 담당자별 실적, 최근 6개월 계약/결제 추이, 등록·수정·삭제 변동 로그로 대체했습니다.
(변동내역은 실제 DB가 없는 데모 특성상 새로고침하면 초기화됩니다 — 실서비스 전환 시 Supabase
트리거로 교체하면 영구 보관됩니다.)

**3) 대시보드**: 상단 연체 배너의 '실시간' 배지를 제거했습니다.

**4) DB관리**: 전환된 리드는 상태가 '전환 완료'로 표시되도록 라벨을 변경했고, 리스트에 '신청분류'
드롭다운(개인회생/개인파산)을 추가해 고객관리 상단 탭 분류에 사용하도록 했습니다. 이름 아래
'추정유형' 텍스트는 제거했습니다(신청분류 드롭다운으로 대체).

⚠️ 요청하신 옵션은 "개인회생/파산" 두 글자였는데, 화면 전체에서 이미 "개인파산"을 표준 표기로
쓰고 있어(사건유형 CaseType 자체가 '개인회생'|'개인파산') 일관성을 위해 '파산' 대신 '개인파산'으로
넣었습니다.

**5) 고객관리**: 목록 상단에 **개인회생/개인파산 필터 탭**을 추가해 DB관리에서 지정한 신청분류로
분류되도록 했고(계약관리 필터 패턴과 동일하게 '전체' 탭도 함께 넣었습니다), 페이지네이션을
**5명씩**으로 변경했습니다. 계약현황은 배지(칩) 대신 **일반 텍스트**로 표기하도록 바꾸고, 목록에
**계약금액·미수금** 컬럼을 명시적으로 추가했습니다. **수정 버튼을 누르면 도원 Admin처럼 팝업창이
뜨고**, 그 안에서 상담일지 전 항목 작성은 물론 계약 관련 메모(계약관리 사건 메모와 연동)와 서류
체크리스트 체크까지 한 화면에서 처리할 수 있습니다.

## v4 업데이트 — 메뉴/기능 1차 정리 요청 반영

디자인 골격(v3)을 유지한 채, 실제 사용 흐름에 맞춘 메뉴·기능 정리 요청을 반영했습니다.

- **메뉴**: DB관리·고객관리 위치를 도원 Admin과 동일하게 맞바꾸고(DB관리가 먼저), '사건관리'를
  '계약관리'로 명칭 통일. 일정관리 단독 메뉴는 없애고(대시보드 캘린더·사건상세 일정으로 충분),
  도원 Admin과 동일한 기능의 **내부 게시판**(공지 고정/순서, 검색, 파일 첨부)을 새로 추가
- **대시보드**: 계약·결제 추이, 미수금 추심 우선순위, 다가오는 기일·제출기한, 담당자별 실적
  카드를 제거해 화면을 더 간결하게 정리(핵심 KPI·분납/기일 캘린더·기간별 통계·구성비 차트는 유지)
- **고객관리**: 유입경로 필터·컬럼과 이메일 필드를 제거하고(메타광고 단일 채널이므로), 도원
  Admin처럼 **행을 클릭하면 하단에 고객 상세 패널이 열려** 분납관리·전자계약서·삭제까지 바로
  처리할 수 있도록 재구성. 컬럼 순서도 등록일·이름·연락처 순으로 통일
- **DB관리**: 시간대 구분과 재콜(카운트) 기능을 제거하고, 이름 표기를 '이름(저장형식)'에서
  순수 이름으로 단순화. 담당자는 자유입력 대신 드롭다운으로, 상태도 드롭다운 하나만 남기고
  그 아래 있던 상태 배지 박스는 제거
- **계약관리**: 메뉴명만 '사건관리'→'계약관리'로 변경(그 외 화면 구성은 동일)
- **입금·분납**: 목록에서 사건번호 컬럼을 제거(의뢰인명 기준으로만 검색)
- **공통**: 모든 목록의 이메일 필드를 제거하고, 등록일·이름·연락처가 함께 있는 목록은 이 순서로
  컬럼을 통일

**추가로 반영한 것** (요청 범위를 넘지 않는 선에서, 도원 어드민과 비교해 있으면 좋겠다고 판단한 것만):
- 고객관리 인라인 수정의 담당자 필드도 자유입력 → 드롭다운으로 통일(DB관리와 동일한 방식)
- 고객 상세 패널에 도원 Admin과 동일한 '삭제' 버튼 + 확인 모달(`ConfirmDelete`)을 추가해
  연결된 계약·분납 데이터까지 함께 정리되도록 함

## v3 업데이트 — 도원 Admin과 동일한 디자인으로 전면 재작업

이전 버전은 로피(LawFee) PG사 자료의 디자인(네이비/골드 톤, 로피식 카드·배지)을 참고해
만들었지만, **디자인은 도원 Admin과 완전히 동일하게, 기능만 다르게** 가져가기로 방향이
바뀌어 아래와 같이 전면 재작업했습니다.

- 컬러 토큰(navy/brand/gold 등 커스텀 팔레트)을 모두 제거하고, 도원 Admin과 동일하게
  **Tailwind 기본 팔레트(slate/blue/emerald/amber/red)만** 사용
- `components/ui/Primitives.tsx`에 도원 Admin `components/ui.tsx`의 Card/Button/Badge/
  PageHeader/Input/Select/Modal/SearchBox/StatusTabs/Pagination을 **그대로 이식**
- 사이드바(`components/layout/Sidebar.tsx`)를 도원 Admin과 동일한 구조(고정 248px 사이드바
  + 모바일 슬라이드 드로어, lucide-react 아이콘)로 교체 — 메뉴/브랜딩만 로파워에 맞게 변경
- 상단 헤더(`components/layout/Header.tsx`)를 신규 추가 — 고객 검색, 알림벨(연체 분납 +
  기일·제출기한), 신규 DB 펄스 배지 등 도원 Admin 헤더 기능을 회파산 도메인에 맞게 재구성
- 대시보드를 도원 Admin과 동일한 구조로 재구성: 기간 선택(DateRangePicker) + KPI 카드 4종 +
  **분납 캘린더 / 기일·제출기한 캘린더 듀얼 월간 캘린더**(날짜 호버 미리보기, 클릭 고정 팝업)
- 고객관리/DB관리/사건관리/입금·분납 목록 화면을 도원 Admin과 동일하게 **모바일 카드 리스트 +
  데스크톱 테이블**의 반응형 이중 레이아웃으로 재구성 (`admin-responsive-table` 포함)
- 사건 상세 화면을 도원 Admin의 고객 상세 패널과 동일한 정보 그리드(`bg-slate-50` 카드) 스타일로 재구성

이전 v2에서 추가했던 기능(직원1/2/3 데모 표기, DB관리 페이지, 게시판형 고객관리, 서류
체크리스트, 결제수단 필드, 전역 상태 스토어)은 그대로 유지됩니다 — 이번 업데이트는 **디자인
전면 교체**이며 기능 목록은 바뀌지 않았습니다.

## 중요 — 로컬에서 처음 실행하기 전에

이 코드는 npm 레지스트리 접근이 차단된 클라우드 샌드박스에서 **`npm install`/빌드를 한 번도
실행하지 못한 상태로** 작성되었습니다. TypeScript 컴파일러(strict 모드)로 문법·타입 오류는
자체 검증했지만, 실제 Next.js 빌드 파이프라인으로는 검증되지 않았으니 로컬에서 아래 순서로
실행하면서 혹시 나오는 사소한 오류(패키지 버전 궁합 등)는 확인해주세요.

```bash
npm install
npm run dev
# http://localhost:3000
```

Node.js 18.18 이상을 권장합니다.

## 기술 스택

- Next.js 14.2 (App Router) + React 18 + TypeScript (strict)
- Tailwind CSS 3.4 — 커스텀 토큰 없이 Tailwind 기본 팔레트만 사용(도원 Admin은 Tailwind 4를
  쓰지만, 기본 팔레트 값 자체는 동일해 시각적으로 같은 결과가 나오면서도 빌드 리스크가 낮은
  3.4를 유지했습니다. 정말 v4까지 맞추고 싶으시면 알려주세요.)
- `lucide-react` / `clsx` / `tailwind-merge` — 도원 Admin과 동일한 아이콘·클래스 병합 유틸
- 외부 차트 라이브러리 없음 — SVG 대신 순수 CSS(conic-gradient, flex 높이)로 도넛/막대 차트 구현
- 실제 백엔드 없음 — `lib/mock-data.ts`가 결정론적 시드(seed) 기반으로 의뢰인 64명·사건 78건·
  DB리드 34건·분납/일정 데이터를 생성합니다.

## 폴더 구조

```
app/
  page.tsx              대시보드 (기간선택 KPI + 분납/기일 듀얼 캘린더 + 기간별 통계)
  db/page.tsx             DB관리 (상담 리드 → 고객 전환, 담당자·상태 드롭다운, 콜 관리 경고)
  db/detail/page.tsx      구 상세 DB관리 주소 호환용 → /db 리다이렉트
  clients/page.tsx        고객관리 (목록 + 클릭 시 하단 상세패널: 분납관리/전자계약서/삭제)
  cases/page.tsx           계약관리 목록 (검색/필터, 모바일 카드뷰)
  cases/[id]/page.tsx      계약 상세 (정보 그리드, 절차 타임라인, 입금내역, 서류 체크리스트)
  billing/page.tsx        입금·분납 관리
  settlements/page.tsx     정산 (계약매출/실매출/미수금 + 담당자별 정산 + CSV 내보내기)
  changes/page.tsx         기간별 변동내역 (카테고리별 등록/수정/삭제 로그)
  analytics/page.tsx       데이터집계 (담당자별 실적 + 6개월 추이 + 유형/단계 분포)
  board/page.tsx           내부 게시판 (공지 고정/순서, 검색, 파일 첨부)
components/
  layout/Sidebar.tsx      고정 사이드바 + 모바일 드로어 (도원 Admin 구조 그대로)
  layout/Header.tsx       검색 + 알림벨 + 신규DB 배지 (신규)
  ui/Primitives.tsx       Card/Button/Badge/PageHeader/Input/Select/Modal(크기 옵션)/SearchBox/
                          StatusTabs/Pagination — 도원 Admin ui.tsx 이식
  ui/DateRangePicker.tsx  기간 선택 캘린더 — 도원 Admin 이식
  ui/ConfirmDelete.tsx    삭제 확인 모달 — 도원 Admin confirm-delete.tsx 이식
  ui/DocumentChecklist.tsx 서류 체크리스트(계약 상세 화면 + 고객수정 팝업 공용)
  ui/Badge.tsx            도메인 전용 배지(사건유형/상태/DB상태)
  charts/MonthCalendar.tsx 월간 일정 캘린더(호버 미리보기 + 클릭 고정) — 도원 Admin 이식
  charts/                 도넛/막대(TrendBarChart)/스택바 차트(색상만 slate/blue 팔레트로 조정)
lib/
  utils.ts                cn() 클래스 병합 유틸 (도원 Admin과 동일)
  types.ts                도메인 모델 (Client, CaseRecord, DbLead, Installment, BoardPost,
                          ConsultationInfo/AssetRow/DebtRow 등 상담일지 타입 …)
  consultation.ts         상담일지 변제계획 자동계산(computeRepaymentPlan) + 자산/채무 초기값
  mock-data.ts            시드 기반 샘플 데이터 생성 + 조회 헬퍼
  documents.ts            서류 체크리스트 템플릿 (서류제출안내문 15개 항목)
  store.tsx               전역 상태(Context) — DB 전환·분납 편집·게시판 CRUD·변동내역 로그 등
  period-engine.ts         년/월/주/일 기간 이동·집계 엔진
  dashboard.ts             대시보드/데이터집계 공용 집계 (연체 리스트, 절차단계 분포, 담당자 실적 등)
  format.ts                금액/날짜/증감률 포맷 유틸
```

## 도원 Admin에서 그대로 가져온 것 / 로파워에 맞게 새로 설계한 것

**그대로 이식 (디자인)**
- 컬러 팔레트(slate/blue/emerald/amber/red), Card/Button/Badge 등 UI 프리미티브, 사이드바·
  헤더 레이아웃 구조, 모바일 카드/데스크톱 테이블 반응형 패턴, 월간 캘린더(호버+핀 팝업) UI

**회생/파산 도메인에 맞게 새로 설계 (기능)**
- 사건유형(개인회생/개인파산)과 9단계 절차 파이프라인(상담접수→…→종결)
- DB관리(상담 리드) → 고객 전환 흐름, 서류 체크리스트, 결제수단(단순분납/카드할부/로펌금융조합분납)
- 도원 Admin의 PG 정산·이폼사인 계약서·사채업체 조율 등 본업(불법사채) 전용 기능은 제외 —
  요청하신 대로 "회생/파산 사건 관리" 범위에만 집중

## 다음 단계 (실서비스 연동 가이드)

1. **DB 연동**: `lib/mock-data.ts`의 export들과 `lib/store.tsx`의 `setState` 호출부를
   Supabase 쿼리/뮤테이션으로 교체하세요. `lib/types.ts`의 인터페이스가 그대로 테이블 스키마
   초안으로 쓸 수 있게 필드명을 설계해뒀습니다.
2. **기존 도원 Admin과의 통합**: 디자인 시스템이 이제 동일하므로, 이 프로젝트를 도원 Admin
   저장소의 라우트 그룹(예: `app/(lawpower)/...`)으로 옮기면 `components/ui/Primitives.tsx`를
   도원 Admin의 `components/ui.tsx`로 그대로 바꿔치기할 수 있습니다 — 두 프로젝트가 같은
   컴포넌트 계약을 쓰기 때문에 병합 리스크가 낮습니다.
3. **인증/권한**: 현재 로그인·권한 분리가 없습니다(도원 Admin은 Supabase Auth 사용). 실사용
   전 담당자별 접근 제어가 필요합니다.
4. **민형사/사채 확장**: 이후 민형사·불법사채구제 사건까지 확장할 경우 `CaseType`에 유형을
   추가하고 유형별 절차 단계(`STAGE_LABELS`)만 새로 정의하면 대시보드/목록/상세 화면 구조는
   대부분 재사용 가능하도록 설계했습니다.

## 참고용 정적 미리보기

npm 없이 디자인만 빠르게 보고 싶다면 별도로 전달된 `dashboard-preview.html`
(빌드 불필요, 브라우저로 바로 열람 가능)을 확인해주세요. 실제 데이터 연동 로직은
포함되어 있지 않은 순수 시각적 목업입니다.

---

## v17 변경사항 (2026-09-23)

- 별도 `상세 DB관리` 메뉴를 제거하고 `/db` 상단에 상담 초기 단계부터 법원/워크아웃/파산까지 통합 진행현황 보드를 배치했습니다.
- 통합 단계: `신규디비`(신청완료+상담대기), `부재`(부재대기+부재관리), `설득필요`(재설득요망+최종설득실패) 및 기존 착수/서류/법원/워크아웃 단계.
- 제거 단계: 미상담, 접수후취소, 1차서류안내, 중복수임, 보유, 플랜, 연체 워크아웃, 착수 추후납부, 서류착수, 차단대기.
- DB 고객리스트에 `진행단계`와 `예약일시`를 직접 편집할 수 있게 했고, 진행단계가 `예약`인 경우 담당자 예약 알림을 상단 헤더 알림벨에서 확인할 수 있습니다.
- `상세 DB관리`의 옛 주소 `/db/detail`은 기존 북마크 호환을 위해 `/db`로 리다이렉트합니다.
- 본인신용정보 열람서비스 링크를 `https://www.credit4u.or.kr:2443/debtcheck`로 변경했습니다.
- 상담일지 기대출리스트는 `.html/.htm` 업로드를 지원합니다. credit4u 채무현황 HTML의 `#debtListTable`을 우선 파싱하여 대출종류/기관명/발생일자/금액을 자동 리스트업하며, 금액은 원본 표의 천원 단위를 내부 원 단위로 정규화합니다. 해당 표가 없는 저장본은 `#grid2ListTable`을 fallback으로 읽습니다.

---

## v22 — 고객/계약 통합 + 개인 대시보드 개편

- 사이드바에서 `고객관리`, `입금·분납`, `게시판` 메뉴 제거.
  - `/clients` → `/cases`, `/billing` → `/cases`, `/board` → `/` 리다이렉트로 기존 북마크 호환 유지.
- 고객관리에서 유지하기로 한 `분납관리`, `전자계약서`, `서류안내문 전송`을 계약관리 상세로 이동.
- 계약관리 상세의 `절차 진행 단계`, `서류체크리스트`, `일정` UI 제거.
- 계약관리 목록의 `전체 단계 ~ 종결` 단계 필터와 단계 컬럼 제거.
- DB관리 상단에 `계약금 / 납부금 / 미수금` 요약 추가. 담당자 필터와 연동되며 미수금은 빨간색 표시.
- 대시보드는 `CURRENT_STAFF` 개인계정 기준으로 재설계:
  - 당일신규 DB(오늘 접수 + 문자/인사 완료 메모)
  - 부재컨택 필요 DB
  - 재통화약속 DB
  - 상담중인 DB
  - 상담완료 DB
  - 일/주/월/기간설정 계약건
  - 재통화약속 / 상담 중 / 고려중 투두리스트
- 실제 로그인 연동 전에는 `CURRENT_STAFF = "박형원"`을 로그인 사용자로 간주하며, 추후 인증 세션 사용자로 교체하면 개인 실적 범위가 자동으로 따라가도록 구성.

---

## v26 — 직원계정관리 / 세부 권한 / 동적 담당자

- 최종관리자 전용 `/staff-accounts` 추가.
- 최종관리자가 Supabase Auth 직원/관리자 계정을 어드민에서 생성·활성화·비활성화·비밀번호 변경·삭제 가능.
- `profiles.permissions` JSONB와 `profiles.is_work_staff` 추가.
- 대시보드/DB/계약/정산/설정/변동내역/데이터집계 38개 세부 권한을 수동 체크 가능.
- 직원계정은 기본 본인 담당 데이터 범위, `*_view_all` 권한으로 전사 범위 확장.
- 프론트 메뉴/버튼 제어 + Supabase RLS + JSONB update trigger를 함께 적용.
- 실무담당자 목록을 고정 배열에서 `profiles.is_work_staff=true` 기반 동적 목록으로 변경.
- 현재 운영 기준 fallback: 강이삭, 박형원. 홍성원 개발자 최종관리자는 실무담당 목록에서 제외.
- 새 계정을 `실무 담당자로 사용`으로 만들면 DB관리 담당자 버튼/드롭다운과 정산설정 목록에 자동 반영.
- 직원명 변경 시 기존 DB/고객/계약 assignedStaff 및 정산요율 키를 RPC로 함께 변경.
- 계정 영구삭제는 해당 직원에게 배정된 DB/고객/계약이 남아 있으면 차단.
- 직원계정 생성/수정/삭제를 변경이력 `설정` 카테고리에 기록.
- 직원계정 관리 API는 서버 전용 `SUPABASE_SERVICE_ROLE_KEY` 사용.
- 적용 SQL: `supabase/migrations/002_staff_accounts_permissions.sql`.

## v27 — 2026-09-24
- Google Sheet `DB가공` 신규행 자동수집 webhook 추가.
- 기존 행 미수집: Apps Script 최초 setup 시 현재 마지막 행을 기준점으로 저장.
- 시트 8열 매핑: 인입 시기/광고명/성함/휴대폰/이메일/채무규모/월소득/상담희망시간.
- Google Sheet 행 중복방지 테이블 및 import RPC 추가.
- 활성 실무담당자의 신규 DB 라운드로빈 자동배정 추가.
- 현재 기본 순서: 강이삭(10) → 박형원(20); 홍성원은 실무담당 제외.
- 직원계정관리에서 신규 DB 자동배정 참여 여부/배정 순서 직접 관리.
- 최종관리자의 기존 DB 담당자 수동 변경 권한은 기존과 동일하게 유지.

## v27.3 — DB 고객정보 삭제

- DB관리 고객 행 맨 우측에 `삭제` 버튼을 추가했습니다(모바일/데스크톱 공통).
- 삭제 전 `삭제 확인` 팝업에서 `아니오 / 예, 삭제`를 반드시 선택하도록 했습니다.
- 삭제 시 해당 `app_leads` DB 리드와 그 안의 상담일지·메모·예약정보가 삭제됩니다.
- 이미 계약관리로 전환된 DB 리드를 삭제해도 계약관리의 고객·계약 데이터는 유지됩니다.
- 직원계정관리의 DB관리 권한에 `DB 고객정보 삭제(db.delete)`를 추가했습니다. 기본 직원 및 실무 전체 프리셋에는 위험 권한이라 자동 포함하지 않습니다.
- Supabase `004_db_lead_delete_permission.sql`을 추가했습니다. 최종관리자는 항상 삭제 가능하고, 직원은 `db.delete` 권한 및 담당범위 조건을 만족할 때만 삭제할 수 있습니다.
